{
  "name": "printflow-printer-agent",
  "version": "0.3.0",
  "description": "PrintFlow Printer Agent — connects Flashforge printers on your local network to PrintFlow.",
  "private": true,
  "type": "module",
  "bin": { "printflow-agent": "dist/cli.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js run"
  },
  "dependencies": {
    "koffi": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5"
  }
}
